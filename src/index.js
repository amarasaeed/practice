import React from 'react';
import ReactDOM from 'react-dom/client';
import Backgroundclr from './Components/Color/Color';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Backgroundclr/>
   

  </React.StrictMode>
);
